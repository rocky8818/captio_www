# ============================================================
#  captio.tech — GitHub Actions: Deploy automático
#  Archivo: .github/workflows/deploy.yml
#
#  Secrets necesarios en tu repo (Settings → Secrets):
#    SSH_PRIVATE_KEY  → llave privada SSH (la que corresponde
#                       a la pública en /home/deploy/.ssh/authorized_keys)
#    DROPLET_IP       → IP pública de tu DigitalOcean Droplet
# ============================================================

name: Deploy a DigitalOcean

on:
  push:
    branches:
      - main   # Se activa al hacer push a main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Bajar el código
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Instalar Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3. Instalar dependencias
      - name: Instalar dependencias
        run: npm ci

      # 4. Build de Astro → genera carpeta /dist
      - name: Build Astro
        run: npm run build

      # 5. Configurar SSH
      - name: Configurar SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.DROPLET_IP }} >> ~/.ssh/known_hosts

      # 6. Subir /dist al droplet con rsync
      - name: Deploy al Droplet
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no" \
            ./dist/ \
            deploy@${{ secrets.DROPLET_IP }}:/var/www/captio.tech/html/

      # 7. Recargar Nginx (opcional, normalmente no hace falta)
      - name: Recargar Nginx
        run: |
          ssh -i ~/.ssh/id_ed25519 \
            -o StrictHostKeyChecking=no \
            deploy@${{ secrets.DROPLET_IP }} \
            "sudo systemctl reload nginx || true"
